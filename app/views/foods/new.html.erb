<h1>Adding new food</h1>


<%= form_for @food, :url => {:action => "new" } do |f|  %>
	
	<%= f.label :name  %>:
	<%= f.text_field :name %><br />
	<br />
	<%= f.label :description %>:
	<%= f.text_area :name %><br />

	<%= f.submit %>
<% end %>